# SPEC C9.2 — Write-back baseline + per-body conflict + surface conflict backups

> **Bối cảnh:** C9.1 (commit dc9a1fe) còn 1 known gap (body-level conflict) và — phát hiện mới khi
> thiết kế spec này — **1 regression thật**:
>
> **(R-C9.1a — REGRESSION, nặng nhất):** sync loop push thành công nhưng **không write-back**
> `updated_at/synced_at` từ row server trả về vào row local. Trước C9.1, re-pull cuối
> `syncFromCache` ghi đè raw nên row tự sạch; C9.1 đổi re-pull thành **merge** (giữ row dirty) →
> row đã-push-thành-công vẫn dirty với baseline cũ → **pass sync kế tiếp tự-conflict với chính
> nội dung của mình** → server-wins → stash một entry RÁC (nội dung y hệt bản live) rồi mới sạch.
> Mỗi offline edit = 1 lần push thừa + 1 conflict giả + 1 stash entry rác. Khi C9.2 đưa stash lên
> UI, user thấy "YOUR EDITS WERE SUPERSEDED" cho edit đã lên server thành công — không chấp nhận được.
>
> **(R-C9.1b — known gap từ commit message):** conflict ở BODY bị catch ở tầng note → stash/
> neutralize NOTE thay vì body → body dirty pin cục bộ; kèm 2 lỗ cùng vùng: body dirty dưới note
> sạch không bao giờ được push (gating theo `isNoteDirty`), và note conflict làm skip toàn bộ
> bodies dù baseline của chúng vẫn hợp lệ.
>
> **(R-C9.1c):** `bbqone_conflict_backups` chỉ ghi, không có UI đọc — "không mất im lặng" mới đúng nửa.
>
> **Failing test đi kèm:** `specs/C9.2-conflict-surface.test.mjs` (Postgres simulator như C9.1).
> RED trên code hiện tại. Gate trong thời gian implement: 9 test cũ xanh + C9.2 đỏ.

---

## PHẦN A — SPEC

### Quyết định thiết kế (đã chốt khi khảo sát source — đừng làm khác)

- **KHÔNG đụng** `SyncConflictDialog.vue` / `conflictDetector.ts` / `src/App.vue` (root) — chúng
  thuộc luồng login-push LocalFirst, ngữ nghĩa khác. Tạo component MỚI `ConflictBackupsDialog.vue`.
- Text UI: tiếng Anh tĩnh, cùng phong cách `SyncConflictDialog.vue` (nó cũng hardcode EN) —
  không mở rộng i18n trong PR này.

### Thay đổi 1/4 — `src/services/sync.service.ts`: restructure sync loop notes

Thay toàn bộ thân `syncDirtyNotesFromList` (hiện tại `sync.service.ts:66-135`) theo cấu trúc sau.
Bốn khác biệt so với hiện tại, mỗi cái giải một lỗi:

1. **Candidates** = note dirty **HOẶC có body dirty** (vá lỗ "body dirty dưới note sạch không bao
   giờ push").
2. **Write-back sau push thành công**: copy `updated_at`/`synced_at` từ row service trả về
   (đã `acceptServerRow` — string server nguyên văn) vào row local → row sạch với baseline đúng
   → hết tự-conflict (R-C9.1a). ⚠ CHỈ copy 2 trường timestamp — KHÔNG copy content fields
   (row secure trả về đang encrypted, đè vào cache plaintext là hỏng).
3. **Note conflict KHÔNG skip bodies** — bodies có baseline riêng, vẫn thử push từng cái.
4. **Per-body recovery**: conflict → stash `'note_body'` + neutralize body + body kế tiếp;
   lỗi network → `break` (bodies còn lại retry pass sau).

```ts
  async syncDirtyNotesFromList(
    notes: Note[],
    noteBodies: NoteBody[],
    folders: Folder[],
    getKey: (folderId: string) => CryptoKey | null,
  ): Promise<number> {
    const byId = new Map(folders.map((f) => [f.id, f]))
    const dirtyBodyNoteIds = new Set(
      noteBodies.filter(isRowDirty).map((b) => b.note_id),
    )
    const candidates = notes.filter((n) => isNoteDirty(n) || dirtyBodyNoteIds.has(n.id))
    let count = 0
    const ts = new Date().toISOString()
    for (const n of candidates) {
      const folder = n.folder_id ? byId.get(n.folder_id) : undefined
      const key = n.folder_id ? getKey(n.folder_id) : null
      const bodies = bodiesForNoteSorted(noteBodies, n.id)
      if (folder?.is_secure) {
        // GIỮ NGUYÊN khối skip-secure-plaintext-khi-không-key hiện có (sync.service.ts:79-88)
        const titlePlain = !isEncryptedEnvelope(n.title)
        const anyBodyPlain = bodies.some(
          (b) => !isEncryptedEnvelope(b.label) || !isEncryptedEnvelope(b.content),
        )
        if ((titlePlain || anyBodyPlain) && !key) {
          continue
        }
      }

      // 1. Push note — chỉ khi CHÍNH note dirty
      if (isNoteDirty(n)) {
        try {
          let title = n.title
          if (folder?.is_secure && key) {
            if (!isEncryptedEnvelope(title)) title = await encryptField(title, key)
          }
          const savedNote = await notesService.update(
            n.id,
            { title, folder_id: n.folder_id, tags: n.tags, synced_at: ts },
            { row: n },
          )
          // Write-back baseline: row sạch, pass sau không tự-conflict với chính mình
          n.updated_at = savedNote.updated_at
          n.synced_at = savedNote.synced_at
          count++
        } catch (e) {
          if (isSyncConflictError(e)) {
            void stashConflictBackup('note', n)
            n.synced_at = n.updated_at // neutralize → re-pull cho bản server thắng
            count++
            // KHÔNG continue: bodies có baseline riêng, vẫn được thử bên dưới
          } else {
            continue // network: retry cả cụm pass sau
          }
        }
      }

      // 2. Push từng body DIRTY (body sạch không push thừa nữa)
      for (const b of bodies) {
        if (!isRowDirty(b)) continue
        try {
          let label = b.label
          let content = b.content
          if (folder?.is_secure && key) {
            if (!isEncryptedEnvelope(label)) label = await encryptField(label, key)
            if (!isEncryptedEnvelope(content)) {
              content = await encryptField(content, key)
            }
          }
          const savedBody = await noteBodiesService.update(
            b.id,
            { label, content, synced_at: ts },
            { row: b },
          )
          b.updated_at = savedBody.updated_at
          b.synced_at = savedBody.synced_at
          count++
        } catch (e) {
          if (isSyncConflictError(e)) {
            void stashConflictBackup('note_body', b)
            b.synced_at = b.updated_at
            count++
            continue
          }
          break // network: bodies còn lại retry pass sau
        }
      }
    }
    return count
  },
```

Ghi chú ngữ nghĩa `count`: giờ đếm per-row (note + từng body + conflict-resolved) thay vì per-note
— `autoSync` chỉ dùng `count > 0` và tổng hiển thị, chấp nhận được.

**Cùng pattern write-back cho calendar** — trong `syncDirtyCalendarEventsFromList`
(hiện tại `sync.service.ts:137-167`), nhánh thành công đổi:

```diff
-        await calendarEventsService.update(
+        const saved = await calendarEventsService.update(
           ev.id,
           { ...giữ nguyên fields..., synced_at: ts },
           { row: ev },
         )
+        ev.updated_at = saved.updated_at
+        ev.synced_at = saved.synced_at
         count++
```

Nhánh catch conflict của calendar giữ nguyên (đã đúng từ C9.1).

Vì sao write-back xong vẫn giữ re-pull merge cuối `syncFromCache`: touch-trigger từ body push
bump note server-side SAU khi note đã write-back → baseline note local cũ đi một nhịp; row lúc đó
**sạch** nên merge cho fresh (normalized, baseline sau-touch) thắng → tự lành. Đó là lý do
write-back (làm row sạch) + merge re-pull (refresh baseline row sạch) phải đi cùng nhau.

Sau thay đổi này, kịch bản "máy B sửa body" hoạt động đúng end-to-end: touch làm note client
conflict (nếu note cũng dirty) → stash note; body conflict → stash body; re-pull → cả hai bản
server hiển thị; user thấy 2 backup trong dialog (Thay đổi 3/4). Lỗ còn CHẤP NHẬN (ghi nhận,
không fix ở đây): network chết giữa chừng sau khi note push OK → bodies dirty dưới note sạch —
đã được vá bằng chính gating `dirtyBodyNoteIds` ở pass sau ✓.

### Thay đổi 2/4 — `src/utils/syncConflict.ts`: backup manager (id + dedupe + list/remove)

Code hiện tại (verbatim, `syncConflict.ts:58-75`):

```ts
export const BBQ_CONFLICT_BACKUPS_KEY = 'bbqone_conflict_backups'
const CONFLICT_BACKUPS_MAX = 20

/** Bản local thua conflict (server-wins nền) — giữ lại để không mất dữ liệu im lặng. */
export async function stashConflictBackup(kind: string, row: unknown): Promise<void> {
  try {
    const chunk = await chrome.storage.local.get(BBQ_CONFLICT_BACKUPS_KEY)
    const raw = chunk[BBQ_CONFLICT_BACKUPS_KEY]
    const list = Array.isArray(raw) ? raw : []
    list.unshift({ kind, row, at: new Date().toISOString() })
    await chrome.storage.local.set({
      [BBQ_CONFLICT_BACKUPS_KEY]: list.slice(0, CONFLICT_BACKUPS_MAX),
    })
  } catch {
    /* best-effort */
  }
}
```

Thay bằng:

```ts
export const BBQ_CONFLICT_BACKUPS_KEY = 'bbqone_conflict_backups'
const CONFLICT_BACKUPS_MAX = 20

export type ConflictBackupKind = 'note' | 'note_body' | 'calendar'

export interface ConflictBackupEntry {
  id: string
  kind: ConflictBackupKind
  /** Bản local thua conflict, nguyên trạng tại thời điểm thua. */
  row: Record<string, unknown>
  at: string
}

function normalizeBackupEntry(raw: unknown): ConflictBackupEntry | null {
  if (typeof raw !== 'object' || raw === null) return null
  const e = raw as { id?: unknown; kind?: unknown; row?: unknown; at?: unknown }
  if (typeof e.kind !== 'string' || typeof e.row !== 'object' || e.row === null) return null
  return {
    // Entry ghi bởi C9.1 (chưa có id) → derive ổn định từ kind + row.id + at
    id: typeof e.id === 'string' && e.id
      ? e.id
      : `${e.kind}:${(e.row as { id?: unknown }).id ?? '?'}:${e.at ?? ''}`,
    kind: e.kind as ConflictBackupKind,
    row: e.row as Record<string, unknown>,
    at: typeof e.at === 'string' ? e.at : '',
  }
}

export async function listConflictBackups(): Promise<ConflictBackupEntry[]> {
  try {
    const chunk = await chrome.storage.local.get(BBQ_CONFLICT_BACKUPS_KEY)
    const raw = chunk[BBQ_CONFLICT_BACKUPS_KEY]
    if (!Array.isArray(raw)) return []
    return raw
      .map(normalizeBackupEntry)
      .filter((e): e is ConflictBackupEntry => e !== null)
  } catch {
    return []
  }
}

export async function removeConflictBackup(id: string): Promise<void> {
  try {
    const list = await listConflictBackups()
    const next = list.filter((e) => e.id !== id)
    if (next.length === 0) {
      await chrome.storage.local.remove(BBQ_CONFLICT_BACKUPS_KEY)
      return
    }
    await chrome.storage.local.set({ [BBQ_CONFLICT_BACKUPS_KEY]: next })
  } catch {
    /* best-effort */
  }
}

/**
 * Bản local thua conflict (server-wins nền) — giữ lại để không mất dữ liệu im lặng.
 * Dedupe theo (kind, row.id): cùng row thua nhiều lần chỉ giữ bản MỚI NHẤT
 * (chặn spam đầy cap 20 khi row conflict lặp qua nhiều pass sync).
 */
export async function stashConflictBackup(kind: ConflictBackupKind, row: unknown): Promise<void> {
  try {
    const list = await listConflictBackups()
    const rowId = (row as { id?: unknown })?.id
    const deduped = list.filter((e) => !(e.kind === kind && e.row?.id === rowId))
    deduped.unshift({
      id: crypto.randomUUID(),
      kind,
      row: row as Record<string, unknown>,
      at: new Date().toISOString(),
    })
    await chrome.storage.local.set({
      [BBQ_CONFLICT_BACKUPS_KEY]: deduped.slice(0, CONFLICT_BACKUPS_MAX),
    })
  } catch {
    /* best-effort */
  }
}
```

2 call site hiện có truyền `'note'`/`'calendar'` — hợp lệ với type mới. `crypto.randomUUID()`
có sẵn ở SW lẫn popup (MV3 = secure context).

### Thay đổi 3/4 — Component mới `src/components/sync/ConflictBackupsDialog.vue`

Component "dumb" theo pattern `SyncConflictDialog.vue` (overlay + dialog + Transition, cùng CSS
variables — copy skeleton style từ đó, đổi class prefix `conflict-backups-`):

```ts
// <script setup lang="ts">
import type { ConflictBackupEntry } from '@/utils/syncConflict'

defineProps<{
  entries: ConflictBackupEntry[]
  visible: boolean
  /** id các entry được phép Restore (row còn tồn tại + không thuộc secure folder). */
  restorableIds: Set<string>
}>()

defineEmits<{
  restore: [entry: ConflictBackupEntry]
  dismiss: [entry: ConflictBackupEntry]
  close: []
}>()
```

Template mỗi entry: nhãn kind (`NOTE` / `NOTE SECTION` / `CALENDAR EVENT`); snippet
`String(entry.row.title ?? entry.row.label ?? entry.row.content ?? entry.row.id)` cắt ~80 ký tự;
`entry.at`. Nút `[ RESTORE MY VERSION ]` (chỉ render khi `restorableIds.has(entry.id)`) emit
`restore`; `[ DISCARD ]` emit `dismiss`. Header `⚠ SYNC CONFLICTS — YOUR EDITS WERE SUPERSEDED`
+ nút close (emit `close`). Dòng phụ: "These local edits lost to newer versions from another
device. Restore to re-apply them, or discard."

### Thay đổi 4/4 — Wiring `src/pages/App.vue`

**4a. State + load sau refresh** (đặt cạnh khối overdue reminder — cùng pattern C7):

```ts
const conflictBackups = ref<ConflictBackupEntry[]>([])
const showConflictBackups = ref(false)

async function maybeShowConflictBackups(): Promise<void> {
  if (!isAuthenticated.value) return
  conflictBackups.value = await listConflictBackups()
  if (conflictBackups.value.length > 0) showConflictBackups.value = true
}
```

Gọi ở 2 chỗ (sửa các dòng hiện có):
- `onMounted` ([pages/App.vue:177](../src/pages/App.vue)):
  `void refreshStoresFromNetwork().then(() => { void maybeShowOverdueReminder(); void maybeShowConflictBackups() })`
- `watch(isAuthenticated)` ([pages/App.vue:198-200](../src/pages/App.vue)): thêm
  `void maybeShowConflictBackups()` cạnh call hiện có.

Nếu trùng lúc với overdue reminder: dialog này render SAU trong template (đè lên) — chấp nhận,
không cần queue.

**4b. `restorableIds`** — computed; entry restorable khi row còn tồn tại trong store VÀ không
thuộc secure folder:

```ts
const restorableBackupIds = computed(() => {
  const ids = new Set<string>()
  for (const e of conflictBackups.value) {
    const rowId = String(e.row.id ?? '')
    if (!rowId) continue
    if (e.kind === 'calendar') {
      if (calendarEvents.events.some((ev) => ev.id === rowId)) ids.add(e.id)
      continue
    }
    const noteId = e.kind === 'note' ? rowId : String(e.row.note_id ?? '')
    const note = notes.notes.find((n) => n.id === noteId)
    if (!note) continue
    if (e.kind === 'note_body' && !notes.bodies.some((b) => b.id === rowId)) continue
    const folder = note.folder_id ? folders.folders.find((f) => f.id === note.folder_id) : null
    if (folder?.is_secure) continue // ⚠ row stash từ SW là bản ENCRYPTED — restore qua
                                    // updateNote sẽ encrypt CHỒNG → hỏng; chỉ cho Discard
    ids.add(e.id)
  }
  return ids
})
```

**4c. Handlers** — restore đi qua đúng store update ĐANG CÓ (C9.1 retry-once bảo kê, user-intent
thắng với baseline mới). CHỦ ĐÍCH chỉ khôi phục **nội dung** (title/tags/label/content/
description/is_done) — không set `folder_id`/`event_date`/`position` từ backup, tránh kéo row về
folder/ngày đã bị máy khác di chuyển:

```ts
async function onConflictBackupRestore(entry: ConflictBackupEntry): Promise<void> {
  const rowId = String(entry.row.id ?? '')
  try {
    if (entry.kind === 'note') {
      await notes.updateNote(rowId, {
        title: String(entry.row.title ?? ''),
        tags: Array.isArray(entry.row.tags) ? (entry.row.tags as string[]) : [],
      })
    } else if (entry.kind === 'note_body') {
      await notes.updateBody(rowId, {
        label: String(entry.row.label ?? ''),
        content: String(entry.row.content ?? ''),
      })
    } else {
      await calendarEvents.updateEvent(rowId, {
        title: String(entry.row.title ?? ''),
        description: String(entry.row.description ?? ''),
        is_done: Boolean(entry.row.is_done),
      })
    }
    await removeConflictBackup(entry.id)
    conflictBackups.value = conflictBackups.value.filter((e) => e.id !== entry.id)
  } catch (e) {
    console.error('[BBQOne] Conflict backup restore failed:', e)
  }
  if (conflictBackups.value.length === 0) showConflictBackups.value = false
}

async function onConflictBackupDismiss(entry: ConflictBackupEntry): Promise<void> {
  await removeConflictBackup(entry.id)
  conflictBackups.value = conflictBackups.value.filter((e) => e.id !== entry.id)
  if (conflictBackups.value.length === 0) showConflictBackups.value = false
}
```

**4d. Template:** thêm `<ConflictBackupsDialog ... />` cuối template (sau overdue dialog) với
props/handlers trên + `@close="showConflictBackups = false"`.

Import mới trong pages/App.vue: `ConflictBackupsDialog`, `listConflictBackups`,
`removeConflictBackup`, `type ConflictBackupEntry`, `computed` (nếu chưa).

### Cái KHÔNG được đổi

- `SyncConflictDialog.vue`, `conflictDetector.ts`, `src/App.vue` (root): nguyên vẹn.
- Note-level conflict recovery (stash `'note'` + neutralize): giữ, chỉ thêm "không skip bodies".
- Re-pull merge cuối `syncFromCache` (C9.1 4c): giữ nguyên — write-back phụ thuộc nó (xem giải
  thích cuối Thay đổi 1).
- Không migration, không dependency mới, không i18n mới.
- `close` dialog KHÔNG xóa backups — chỉ Discard chủ động (hoặc tràn cap 20) mới mất.

### ⚠ FLAG liên đới

1. **R-C9.1a là regression đang chạy trên production-path** (origin/main hiện tại): mỗi offline
   edit sau khi push thành công sẽ push thừa + conflict giả + stash rác ở pass sync kế. Chưa có UI
   đọc stash nên user chưa thấy, nhưng cap 20 đang bị chiếm dần bởi rác → **ưu tiên ship C9.2
   sớm**; nếu C9.2 UI cần thêm thời gian, có thể tách Thay đổi 1 (write-back) ship trước.
2. **Secure folder:** stash từ SW là bản encrypted → `restorableIds` loại secure rows (chỉ
   Discard). Nâng cấp sau: decrypt-preview khi popup có key.
3. Dedupe đổi hành vi stash so với C9.1 — test C9.1 hiện hành chỉ dùng `.some()`, không assert số
   lượng → không vỡ (đã kiểm tra T2b của C9.1 test).

---

## PHẦN B — FAILING TEST

File: `specs/C9.2-conflict-surface.test.mjs` — Postgres simulator cùng pattern C9.1 (trigger
server-clock lệch +137ms, microsecond, `note_bodies_touch_note`, RPC guard string). Thực thi
code thật: syncConflict, notes/noteBodies services, sync.service.

| Case | Nội dung | Hiện tại |
|---|---|---|
| T0 | **R-C9.1a**: push dirty thành công → write-back baseline (row sạch, `synced_at` = server updated_at nguyên văn); pass 2 với cùng mảng → KHÔNG rpc call, KHÔNG stash | **RED** — pass 2 tự-conflict + stash rác |
| T1 | Body dirty dưới note SẠCH vẫn được push; khi body conflict → stash kind `'note_body'` chứa content body + body neutralize + server giữ bản B | **RED** — gating `isNoteDirty` bỏ qua cả cụm, body pin im lặng |
| T2 | Note conflict KHÔNG skip bodies: note stale-conflict (stash `'note'`) nhưng body baseline hợp lệ vẫn push thành công lên server | **RED** — outer catch skip bodies |
| T3 | Dedupe: stash cùng (kind, row.id) 2 lần → 1 entry, giữ bản mới nhất | **RED** |
| T4 | `listConflictBackups` trả entry có `id`; entry legacy C9.1 (không id) được derive id ổn định; `removeConflictBackup(id)` xóa đúng entry | **RED** — chưa export |
| W1-W3 | Static: sync.service có `stashConflictBackup('note_body'`; `ConflictBackupsDialog.vue` tồn tại với emit restore/dismiss; `pages/App.vue` có `listConflictBackups` + `ConflictBackupsDialog` | **RED** |

UI render/restore không harness được bằng Node — verify tay theo checklist PHẦN C.

## PHẦN C — RED→GREEN CRITERIA

Test chuyển FAIL→PASS khi và chỉ khi: sync loop write-back `updated_at/synced_at` sau mỗi push
thành công VÀ candidates gồm note-có-body-dirty VÀ note-conflict không skip bodies VÀ per-body
conflict recovery stash `'note_body'` VÀ syncConflict.ts export list/remove với entry có id +
stash dedupe VÀ `ConflictBackupsDialog.vue` được wire trong `pages/App.vue` theo mục 4.

Checklist verify tay sau GREEN: (1) `vue-tsc` sạch; (2) 9 test cũ vẫn xanh (chạy CẢ suite);
(3) seed `bbqone_conflict_backups` qua DevTools → mở popup: dialog hiện, Restore đẩy nội dung vào
note/event rồi entry biến mất, Discard chỉ xóa entry, Close giữ nguyên backups; (4) backup của
note trong secure folder không có nút Restore; (5) sửa note offline → online → chờ 2 pass autoSync
→ KHÔNG có dialog conflict rác.
