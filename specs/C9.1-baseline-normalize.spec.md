# SPEC C9.1 — Sửa baseline optimistic lock: chuẩn hóa `synced_at := server updated_at`

> **Bối cảnh:** C9 (commit 852885a) bị tắt bằng hotfix `C9_OPTIMISTIC_RPC_ENABLED = false`
> (commit 5a55bd8) vì 3 lỗi thiết kế đã confirm khi re-check:
> (R1) baseline dirty = `synced_at` là timestamp **client tự sinh**, không bao giờ khớp server
> `updated_at` do trigger `retronote_update_updated_at()` đặt `NEW.updated_at := now()` giờ server
> → mọi dirty push BBQ_CONFLICT vĩnh viễn;
> (R2) trigger `note_bodies_touch_note` ([005_note_bodies.sql:161-163](../supabase/migrations/005_note_bodies.sql))
> bump `notes.updated_at` khi push body → rename note sau khi sửa body luôn conflict;
> (R3) RETURNING merge giữ `synced_at` cũ → row "ghost-dirty" vĩnh viễn → C1 merge-guard pin bản
> local, chặn edit từ máy khác.
>
> **C9.1 sửa cả 3 bằng một nguyên tắc:** trường `synced_at` trong cache client = **server
> `updated_at` nguyên văn tại lần client nhận row gần nhất**. Migration 014 giữ nguyên, KHÔNG cần
> migration mới. Cột `synced_at` trên server trở thành informational-only.
>
> **Trạng thái:** **IMPLEMENTED** — test `node specs/C9.1-baseline-normalize.test.mjs` GREEN.

---

## PHẦN A — SPEC

### Nguyên tắc bất biến (đọc trước khi code)

1. `synced_at` client-side = chuỗi `updated_at` **nguyên văn** server trả về lần gần nhất.
2. ⚠ **BẪY MICROSECOND:** Postgres `timestamptz` có độ chính xác micro giây; PostgREST trả
   `"2026-07-03T14:12:22.123456+00:00"`. JS `new Date(x).toISOString()` chỉ giữ mili giây →
   nếu baseline đi qua `Date` round-trip, `WHERE updated_at = p_expected_updated_at` **không bao
   giờ khớp**. Mọi chỗ lưu/truyền baseline phải copy **string as-is**. So sánh dirty
   (`isRowDirty`) dùng `new Date()` hai vế để so lớn/nhỏ thì được — cấm dùng để tạo lại string.
3. Row từ nhánh **local-mode** (anonymous) giữ `synced_at: null` — không normalize (null nghĩa là
   "chưa từng có bản server", đi đường plain update / pushLocalToCloud như cũ).
4. Dirty-detection (`isRowDirty`, `isNoteDirty`, `isCalendarEventDirty` — [sync.service.ts:15-29](../src/services/sync.service.ts))
   **không đổi**: sau chuẩn hóa, row sạch có `updated_at === synced_at` (cùng string) → không dirty;
   edit offline bump `updated_at` → dirty. Ngữ nghĩa tự đúng.

### Thay đổi 1/6 — `src/utils/syncConflict.ts`: helpers mới + sửa baseline + bật flag

**1a.** Bật flag (đang `false` tại dòng 6):

```diff
-export const C9_OPTIMISTIC_RPC_ENABLED = false
+export const C9_OPTIMISTIC_RPC_ENABLED = true
```

**1b.** Thêm `acceptServerRow` (đặt cạnh `expectedServerUpdatedAt`):

```ts
/**
 * Chuẩn hóa row nhận từ server: synced_at := updated_at (string NGUYÊN VĂN từ PostgREST).
 * Đây là nguồn duy nhất của baseline optimistic-lock. KHÔNG đi qua new Date() (mất microsecond).
 * CHỈ áp cho nhánh authenticated — row local-mode giữ synced_at null.
 */
export function acceptServerRow<T extends { updated_at: string; synced_at?: string | null }>(
  row: T,
): T {
  return { ...row, synced_at: row.updated_at }
}
```

**1c.** Đơn giản hóa `expectedServerUpdatedAt` — thay toàn bộ thân hàm hiện tại
([syncConflict.ts:35-39](../src/utils/syncConflict.ts), nhánh dirty/clean cũ dựa trên giả định sai):

```ts
/**
 * Baseline cho bbq_update_*_if_current: sau chuẩn hóa acceptServerRow, synced_at LUÔN là
 * server updated_at last-seen — dùng thẳng cho cả row sạch lẫn dirty.
 * null → chưa từng có bản server (local-mode / chưa push) → plain update, không guard.
 */
export function expectedServerUpdatedAt(row: ServerVersionRow): string | null {
  return row.synced_at ?? null
}
```

**1d.** Thêm clock guard:

```ts
/**
 * Timestamp cho edit offline: luôn LỚN HƠN updated_at/synced_at hiện có ít nhất 1ms —
 * client clock chậm hơn server không làm edit trông "sạch" (mất push).
 */
export function nextLocalUpdatedAt(row: { updated_at: string; synced_at?: string | null }): string {
  const floor =
    Math.max(
      new Date(row.updated_at).getTime(),
      row.synced_at ? new Date(row.synced_at).getTime() : 0,
    ) + 1
  return new Date(Math.max(Date.now(), floor)).toISOString()
}
```

**1e.** Thêm stash backup (best-effort, cho recovery nền — mục 4):

```ts
export const BBQ_CONFLICT_BACKUPS_KEY = 'bbqone_conflict_backups'
const CONFLICT_BACKUPS_MAX = 20

/** Bản local thua conflict (server-wins nền) — giữ lại để không mất dữ liệu im lặng. */
export async function stashConflictBackup(kind: string, row: unknown): Promise<void> {
  try {
    const chunk = await chrome.storage.local.get(BBQ_CONFLICT_BACKUPS_KEY)
    const raw = chunk[BBQ_CONFLICT_BACKUPS_KEY]
    const list = Array.isArray(raw) ? raw : []
    list.unshift({ kind, row, at: new Date().toISOString() })
    await chrome.storage.local.set({
      [BBQ_CONFLICT_BACKUPS_KEY]: list.slice(0, CONFLICT_BACKUPS_MAX),
    })
  } catch {
    /* best-effort */
  }
}
```

### Thay đổi 2/6 — Chuẩn hóa tại 3 service (choke point duy nhất nhận row server)

Import thêm `acceptServerRow` từ `@/utils/syncConflict` vào cả 3 file. Áp `acceptServerRow`
cho **mọi return của nhánh authenticated** (giữ nguyên nhánh local-mode):

**`src/services/notes.service.ts`:**
- `getAll` ([:20](../src/services/notes.service.ts)): `return (data ?? []).map(acceptServerRow)`
- `create` ([:46](../src/services/notes.service.ts)): `return acceptServerRow(data)`
- `update` — cả 2 nhánh: RPC ([:73](../src/services/notes.service.ts))
  `return acceptServerRow(data as Note)` và plain ([:82](../src/services/notes.service.ts))
  `return acceptServerRow(data)`
- `searchFullText` ([:148](../src/services/notes.service.ts)): `return (merged ?? []).map(acceptServerRow)`
  (các nhánh fallback đi qua `getAll` — đã normalize)

**`src/services/noteBodies.service.ts`:** `getAll` (:20), `listByNoteId` (:36), `create` (:64),
`update` cả 2 nhánh (:90, :99) — cùng pattern.

**`src/services/calendarEvents.service.ts`:** `getAll` (:25), `listByDateRange` (:46),
`create` (:83), `update` cả 2 nhánh (:111, :120) — cùng pattern.

Lưu ý: `getAll` order by `updated_at` không đổi; normalize không đụng `updated_at`.

### Thay đổi 3/6 — UI conflict: retry-once với server state (interactive-wins, giải R2)

Thêm vào `OptimisticUpdateOptions` (syncConflict.ts):

```ts
export type OptimisticUpdateOptions = {
  row?: ServerVersionRow
  expectedServerUpdatedAt?: string
  /** UI edit trực tiếp: conflict → refetch server row, retry 1 lần với baseline mới (user intent thắng). */
  retryOnConflictWithServerState?: boolean
}
```

Trong **mỗi** service `update`, nhánh RPC: nếu `throwIfSyncConflict` sắp ném mà
`options?.retryOnConflictWithServerState` → refetch + retry một lần. Pattern cho notes.service
(2 service kia tương tự với field của chúng):

```ts
if (expected !== null && base) {
  let attempt = await supabase.rpc('bbq_update_note_if_current', rpcArgs(expected, base))
  if (attempt.error && isSyncConflictError(attempt.error) && options?.retryOnConflictWithServerState) {
    const { data: fresh, error: freshErr } = await supabase
      .from('notes').select('*').eq('id', id).single()
    if (!freshErr && fresh) {
      // baseline mới = updated_at server hiện tại (string nguyên văn); field không edit lấy theo server
      attempt = await supabase.rpc('bbq_update_note_if_current', rpcArgs(fresh.updated_at, fresh))
    }
  }
  throwIfSyncConflict(attempt.error) // retry vẫn conflict (race hiếm) → SyncConflictError như cũ
  if (attempt.error) throw attempt.error
  return acceptServerRow(attempt.data as Note)
}
```

trong đó `rpcArgs(expectedUpdatedAt, baseRow)` là phần args hiện có ([notes.service.ts:63-70](../src/services/notes.service.ts))
tách thành closure/hàm nhỏ để gọi 2 lần — `updates.*` (intent của user) vẫn ưu tiên, chỉ `base`
đổi thành `fresh`. Cách tách tùy implementer, miễn không lặp 2 khối args chép tay lệch nhau.

**Stores truyền cờ này ở các đường EDIT TRỰC TIẾP** (thêm vào options hiện có):
- [notes.ts:256](../src/stores/notes.ts) `updateNote`: `{ row: prev, retryOnConflictWithServerState: true }`
- [notes.ts:313](../src/stores/notes.ts) `updateBody`: tương tự
- [calendarEvents.ts:161](../src/stores/calendarEvents.ts) `updateEvent`: tương tự
- `secureFolder.ts` (4 chỗ `{ row: n }` / `{ row: b }`): **CÓ** truyền retry (re-encrypt là thao tác
  chủ động của user, phải thắng)

Sync loop nền (`sync.service.ts`) **KHÔNG** truyền cờ — nền đi đường server-wins (mục 4).

Sau thay đổi này, luồng R2 (sửa body → touch bump note.updated_at → rename note) tự lành:
conflict lần 1 → refetch (nhận updated_at sau-touch) → retry → thành công, user không thấy gì.

### Thay đổi 4/6 — Sync nền: conflict = server-wins + stash (không skip im lặng vĩnh viễn)

Trong `syncDirtyNotesFromList` và `syncDirtyCalendarEventsFromList`
([sync.service.ts:65-157](../src/services/sync.service.ts)):

**4a. Đảo thứ tự push notes: NOTE trước, BODIES sau** (hiện tại bodies trước note —
[sync.service.ts:89-122](../src/services/sync.service.ts)). Lý do: push body kích
`note_bodies_touch_note` bump `notes.updated_at` server-side → nếu note push sau sẽ conflict
ngay trong cùng một pass. Note conflict → **skip luôn bodies của note đó** trong pass này
(chúng thuộc "bản thua", re-pull cuối sẽ đồng bộ).

**4b. Catch phân loại** — thay `catch {}` trống hiện tại:

```ts
try {
  // ... push (note trước, bodies sau / event)
  count++
} catch (e) {
  if (isSyncConflictError(e)) {
    // Server có bản mới hơn → bản local dirty là bản thua. Server-wins:
    // stash để không mất im lặng, rồi trung hòa dirty flag để re-pull cuối đè bằng bản server.
    void stashConflictBackup('note', n) // hoặc 'calendar', ev
    n.synced_at = n.updated_at // neutralize: hết dirty → merge ở re-pull cho fresh thắng
    count++ // tính là "đã xử lý" để kích re-pull phía dưới
  }
  /* network / lỗi khác: skip, retry lần sau (giữ nguyên hành vi) */
}
```

(Mutation trực tiếp `n.synced_at` chấp nhận được: mảng này là bản copy đọc từ cache trong
`syncFromCache`, hoặc mảng Pinia trong popup — trung hòa trong Pinia cũng đúng ý nghĩa.)

**4c. Re-pull cuối `syncFromCache` đổi từ ghi đè raw sang MERGE** — hiện tại
[sync.service.ts:196-209](../src/services/sync.service.ts) `chrome.storage.local.set` thẳng
mảng fresh, sẽ đè mất row dirty chưa push được vì lỗi network (đúng bug C1 trong context SW). Đổi:

```ts
await chrome.storage.local.set({
  [NOTES_CACHE_KEY]: mergeFreshWithDirtyLocal(freshNotes, notes, isRowDirty),
  [NOTE_BODIES_CACHE_KEY]: mergeFreshWithDirtyLocal(freshBodies, noteBodies, isRowDirty),
  [CALENDAR_EVENTS_CACHE_KEY]: mergeFreshWithDirtyLocal(freshCalendar, calendarEvents, isRowDirty),
})
```

`mergeFreshWithDirtyLocal` + `isRowDirty` đã có sẵn trong chính file này
([sync.service.ts:26-54](../src/services/sync.service.ts)). Row bị conflict đã được neutralize ở
4b → hết dirty → fresh (bản server thắng) đè vào cache ✓. Row dirty vì network-fail → được giữ ✓.
`freshNotes/freshBodies/freshCalendar` đã normalize sẵn vì đi qua service getAll (Thay đổi 2).

### Thay đổi 5/6 — Clock guard tại 3 offline-fallback trong stores

Thay `const ts = new Date().toISOString()` bằng `nextLocalUpdatedAt(prev)` (import từ
`@/utils/syncConflict`):

- [notes.ts:277](../src/stores/notes.ts) (`updateNote` catch): `const ts = nextLocalUpdatedAt(prev)`
- [notes.ts:335](../src/stores/notes.ts) (`updateBody` catch): `const ts = nextLocalUpdatedAt(prev)`
  — dòng 344 bump note cha dùng cùng `ts` (giữ nguyên logic, chỉ đổi nguồn `ts`)
- [calendarEvents.ts:171](../src/stores/calendarEvents.ts) (`updateEvent` catch):
  `const ts = nextLocalUpdatedAt(cur)` (lưu ý biến trong scope là `cur`, khai báo sau `next.slice()` —
  đặt dòng tính `ts` SAU `const cur = next[idx]`)

### Thay đổi 6/6 — Test C9 cũ: flip 1 assert

`specs/C9-supabase-optimistic-lock.test.mjs` C9.4b hiện assert `C9_OPTIMISTIC_RPC_ENABLED === false`
→ đổi thành `=== true` (message cập nhật "C9.1 đã bật lại"). Nhánh `if (C9_OPTIMISTIC_RPC_ENABLED)`
của C9.5 tự chuyển sang assert RPC — đã viết sẵn, không đụng. C9.1/C9.2/C9.3 (baseline cũ) vẫn pass
với hàm mới (dirty → synced_at; clean → synced_at cùng giá trị updated_at; null → null) — không đụng.

### Cái KHÔNG được đổi

- **KHÔNG migration mới** — RPC 014 giữ nguyên signature; `p_synced_at` vẫn gửi như cũ (cột server
  thành informational, không sao).
- **KHÔNG đổi** `isRowDirty`/`isNoteDirty`/`isCalendarEventDirty`, `mergeFreshWithDirtyLocal`,
  merge-guard trong stores `loadAll` (C1), pendingDeleteCommit (C2/C3/R4), `pushLocalToCloud` (C6).
- **KHÔNG normalize nhánh local-mode** của services (synced_at null là ngữ nghĩa "chưa từng push").
- **KHÔNG** thêm dependency, không đổi UI (SyncConflictDialog để C9.2 — retry vẫn conflict thì
  throw `SyncConflictError` như hiện tại).

### ⚠ FLAG liên đới

1. **Deploy order:** migration 014 đã apply prod (confirm 2026-07-03) → chỉ cần ship client. Nếu
   môi trường khác (dev/staging) chưa apply → RPC lỗi PGRST202 → UI edit throw. Checklist release:
   verify `bbq_update_*_if_current` tồn tại trước khi bật flag ở build cho môi trường đó.
2. **Rows cũ trong cache người dùng thật** (tạo trước C9.1) có `synced_at` kiểu client-ts → lần
   push đầu sau update extension sẽ conflict giả 1 lần → đi đường server-wins 4b → stash + nhận
   bản server. Nếu user có offline edit CHƯA push từ trước update, edit đó vào stash thay vì lên
   server — chấp nhận (một lần, có backup, không mất im lặng). Ghi vào release note nội bộ.
3. **C1 merge-guard + neutralize:** sau 4b, row hết dirty → merge cho fresh thắng — đúng chủ đích.
   Đừng "tối ưu" bỏ neutralize rồi trông cậy re-pull, sẽ tái tạo ghost-dirty pin.

---

## PHẦN B — FAILING TEST

File: `specs/C9.1-baseline-normalize.test.mjs`. Chạy: `node specs/C9.1-baseline-normalize.test.mjs`.

Khác biệt quyết định so với test C9 cũ (xanh giả): mock server là **Postgres simulator** có:
- BEFORE UPDATE trigger: `updated_at := serverNow()` — **giờ server lệch +137ms so với client** và
  format PostgREST **có microsecond** (`.123456+00:00`);
- trigger `note_bodies_touch_note`: update body → bump `notes.updated_at`;
- RPC `bbq_update_*_if_current`: so sánh string `updated_at === p_expected_updated_at`, sai → error
  `P0001 BBQ_CONFLICT` (đúng semantics migration 014).

Test thực thi **code thật** (transpile): `syncConflict.ts`, 3 services, `sync.service.ts`.

| Case | Nội dung | Trạng thái hiện tại (hotfix) |
|---|---|---|
| W1 | `C9_OPTIMISTIC_RPC_ENABLED === true` | **RED** |
| T1 | Pull → offline edit → dirty push qua sync loop **thành công** (server nhận edit) và có dùng RPC | RED khi bật flag mà thiếu normalize (conflict vĩnh viễn — lỗi C9 gốc) |
| T2 | Máy B đã sửa mới hơn → dirty push cũ **bị chặn** (server giữ bản B) + bản thua vào stash + row local hết dirty | **RED** (hotfix LWW đè mất bản B; C9 gốc chặn được nhưng skip im lặng, không stash) |
| T3 | Sửa body (touch bump note) → rename note interactive **thành công** nhờ retry-once | RED khi bật flag mà thiếu retry (lỗi R2) |
| T4 | `synced_at` sau getAll/update giữ **nguyên văn** microsecond `+00:00` (không Date-round-trip) | **RED** (chưa có normalize) |
| T5 | Sau UI update thành công, row trả về hết ghost-dirty (`synced_at === updated_at`) | **RED** |
| W2-W5 | Static: services có `acceptServerRow(`; sync loop note-trước-bodies; stores dùng `nextLocalUpdatedAt`; `syncFromCache` re-pull dùng `mergeFreshWithDirtyLocal` | **RED** |

T2 là case phân thắng bại giữa 3 trạng thái: hotfix-LWW (fail — đè), C9-gốc (fail — không stash,
không neutralize), C9.1 (pass). T1+T3 chặn việc "bật lại flag mà không sửa baseline".

## PHẦN C — RED→GREEN CRITERIA

Test chuyển FAIL→PASS khi và chỉ khi: flag bật lại VÀ mọi row nhận từ server (getAll/create/update,
authenticated) được chuẩn hóa `synced_at := updated_at` nguyên văn (giữ microsecond) VÀ baseline
RPC = `synced_at ?? null` VÀ sync loop push note trước bodies + conflict nền đi đường
server-wins-stash-neutralize + re-pull merge VÀ UI update retry-once với server state khi conflict
VÀ offline fallback dùng `nextLocalUpdatedAt`.
